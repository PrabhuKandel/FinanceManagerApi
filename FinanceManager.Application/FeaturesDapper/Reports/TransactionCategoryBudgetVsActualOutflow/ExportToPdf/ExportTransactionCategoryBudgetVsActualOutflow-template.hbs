<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Budget vs Actual Report</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 30px;
        }

        h1, h2 {
            text-align: center;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        table, th, td {
            border: 1px solid #333;
        }

        th, td {
            padding: 8px;
            text-align: right;
        }

        th {
            background-color: #f2f2f2;
        }

        td.left {
            text-align: left;
        }

        tr.over-budget {
            background-color: #ffcccc;
        }
    </style>
</head>
<body>
    <h1>Budget vs Actual Outflow Report</h1>
    <h2>Period: {{ formatDate periodStart "dd/MM/yyyy"}} to {{ formatDate periodEnd "dd/MM/yyyy"}} | Period Type: {{periodType}}</h2>

    <table>
        <thead>
            <tr>
                <th>Transaction Category</th>
                <th>Budget Amount (Rs)</th>
                <th>Actual Amount (Rs)</th>
                <th>Remaining Budget (Rs)</th>
                <th>Budget Usage (%)</th>
            </tr>
        </thead>
        <tbody>
            {{#each data}}
            <tr>
                <td class="left">{{TransactionCategoryName}}</td>
                <td>{{BudgetAmount}}</td>
                <td>{{ActualAmount}}</td>
                <td>{{RemainingBudget}}</td>
                <td>{{ formatPercent   BudgetUsagePercent}}%</td>
            </tr>
            {{/each}}
        </tbody>
    </table>
</body>
</html>
